
# Link Shortener

## Setup
1. Install Node.js and MongoDB
2. Clone project
3. Run `npm install`
4. Create `.env` from `.env.example`
5. Run `npm start`

## API
POST /api/shorten
{
  "longUrl": "https://example.com"
}
